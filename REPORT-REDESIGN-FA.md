# گزارش بازطراحی صفحهٔ چت ژینو و پنل مدیریت (نسخهٔ نهایی)

شاخهٔ کاری: `arena/01a0b516-zheno-website` — **هیچ Commit / Push / PR / Merge انجام نشده** (طبق خواستهٔ شما).
تغییر دیتابیسی: **هیچ** (`supabase/migrations/` و `supabase/functions/` دست‌نخورده‌اند).

---

## ۱) فایل‌های تغییرکرده

| # | فایل | چه تغییری کرد |
|---|---|---|
| 1 | `src/admin/pages/AdminDashboardPage.tsx` | صفحهٔ اصلی پنل: هدر برند کوچک + نوار باریک آمار + **۷ کارت مستقل و هم‌سطح** (آیکون، عنوان، توضیح، دکمهٔ گرد) + دو میان‌بر جدا (قیمت‌ها/تصاویر) + «تنظیم کارت‌ها» و فهرست‌های جمع‌شونده |
| 2 | `src/admin/admin.css` | کلاس‌های تازهٔ `adm-card*`، `adm-stat-strip`، `adm-stat-chip`، `adm-shortcut*`، `adm-fold*`، `adm-acc*` (فقط افزودنی) |
| 3 | `src/admin/pages/AdminAssistantPage.tsx` | آکاردئون‌های «ربات ژینو»: کاشی آیکون، عنوان/توضیح مرتب‌تر، فلش چرخان، نوار ذخیرهٔ ظریف‌تر — **منطق و قابلیت‌ها بدون تغییر** |
| 4 | `src/components/assistant/AssistantChat.tsx` | کارت محصول داخل پاسخ + دکمهٔ گرد «افزودن به سبد» + **درگاه تأیید** (بله/نه) |
| 5 | `src/components/assistant/assistant.css` | سبک کارت محصول، دکمهٔ افزودن، کادر تأیید، سایهٔ ظریف‌تر حباب‌ها، چیدمان موبایل |
| 6 | `src/components/assistant/useAssistantChat.ts` | عبور `products`/`cartOffer` به پیام‌ها + `acceptCartOffer` / `dismissCartOffer` از مسیر همان `CartContext` فروشگاه (+ `retry()` که در UI استفاده نمی‌شود) |
| 7 | `src/services/assistant/types.ts` | تایپ‌های `AssistantCartOffer`، `AssistantCartOfferState` و فیلدهای اختیاری `products` / `cartOffer` |
| 8 | `src/services/assistant/engine.ts` | قابلیت تازهٔ `add-to-cart` (فقط پیشنهاد)، کارت محصول در پاسخ‌های قیمت/موجودی، معرفی محصولات، پیشنهاد دسر، بودجه و ترکیب طعم؛ **پاسخ صادقانه برای محصول ناموجود** (بدون جایگزینی بی‌اجازهٔ طعم دیگر) |
| 9 | `scripts/smoke.mjs` | دو سوئیت تست تازه: `assistant shop` (افزودن با تأیید + نبود Checkout خودکار) و `assistant out-of-stock` (ناموجودی) |
| 10 | `scripts/verify-admin.mjs` | **۹ سنجش قراردادی تازه** تا داشبورد ۷ کارت مستقل و «ربات ژینو» جمع‌شونده بماند (برگشت به شبکهٔ شلوغ، تست را می‌شکند) |
| 11 | `ASSISTANT_AI.md` | مستندسازی قابلیت تازه: ردیف «افزودن به سبد با تأیید» + بخش ۶٫۰۲ (قاعدهٔ ۴ مرحله‌ای و محل تست) |
| — | `PLAN-REDESIGN-FA.md` | طرح اولیه (قبل از اجرا) — مستند |

**دست‌نخورده (با `git diff --stat` بررسی شد، خروجی خالی):**
`src/App.tsx` (Routing) · همهٔ `src/pages/*` فروشگاه · `src/hooks/useCart.ts` · `src/context/CartContext.tsx` ·
`src/components/assistant/useAssistantSpeech.ts` · `src/services/voiceSettings.ts` · `src/services/assistant/voice.ts` · `supabase/*`.

---

## ۲) قابلیت‌های تکمیل‌شده

### پنل مدیریت
- **۷ کارت مستقل و هم‌سطح**: محصولات · سفارش‌ها · ربات ژینو · تنظیمات · موجودی · دستور تهیه · محتوای سایت.
  هیچ‌کدام زیرمجموعهٔ دیگری نیست؛ هر کارت آیکون، عنوان کوتاه، یک خط توضیح و دکمهٔ گرد دارد.
- شبکه: ۳ ستون دسکتاپ، ۲ تبلت، ۱ موبایل؛ گوشهٔ نرم `1.25rem` و سایهٔ `0 1px 2px` بسیار ظریف؛ هاور با حلقهٔ زرشکی.
- آمار (همان ۶ شاخص قبلی) در یک نوار باریک بالای کارت‌ها؛ «تنظیم کارت‌ها» یک دکمهٔ کوچک در همان نوار.
- «قیمت‌ها» و «تصاویر محصولات» دو لینک ظریف و **جدا** زیر کارت‌ها (هر دو به همان صفحهٔ محصولات؛ قرارداد تست پنل هم حفظ شد).
- «ربات ژینو» همان صفحهٔ مستقل با سه دستهٔ جمع‌شونده (صدا / رفتار / وضعیت سرویس) — هیچ قابلیتی حذف نشد.

### صفحهٔ چت
- **کارت محصول**: نام، قیمت واقعی، وزن، وضعیت موجودی («موجود» / «فقط ۳ عدد» / «ناموجود») — همه از کاتالوگ همان لحظه، بدون درخواست شبکهٔ اضافه.
- **افزودن به سبد فقط با تأیید**: دکمهٔ «افزودن به سبد» → کادر تأیید («… به سبد خرید اضافه شود؟» با «بله، اضافه کن» / «نه»).
- **بدون Checkout خودکار**: بعد از افزودن فقط پیام تأیید + لینک «دیدن سبد خرید» و جملهٔ صریح «ثبت سفارش و پرداخت با خودتان است».
- گفتن «ژله توت فرنگی را به سبد خرید اضافه کن» همان جریان را می‌سازد (قابلیت `add-to-cart`) با قیمت و موجودی واقعی.
- قیمت/موجودی هنگام افزودن **دوباره از کاتالوگ** خوانده می‌شود؛ اگر همان لحظه ناموجود شده باشد، چیزی اضافه نمی‌شود.
- **ناموجودی (جدید)**: اگر محصول ناموجود باشد، ربات صریحاً می‌گوید «پیدا کردم، ولی همین لحظه موجود نیست و چیزی به سبد اضافه نکردم» و طعم‌های موجود را فهرست می‌کند — **بدون پیشنهاد افزودن** و بدون جایگزین‌کردن بی‌اجازهٔ طعم دیگر.
- «نه» → سبد دست‌نخورده + «چیزی به سبد اضافه نکردم».
- ظاهر: سایهٔ حباب‌ها ظریف‌تر، کارت و دکمه‌ها هم‌پالت برند (زرشکی `wine-900` روی شیری `cream`)، در موبایل کارت تمام‌عرض و دکمهٔ گرد بزرگ‌تر.
- **کنترل‌های صدا طبق تصمیم شما جابه‌جا نشدند**؛ همهٔ قابلیت‌های صدا (ابری + مرورگر، تست، سرعت، انتخاب صدا) حفظ شده‌اند.

### آنچه عمداً انجام نشد
- دکمهٔ «تلاش دوباره» در چت: قرارداد موجود (`scripts/smoke.mjs` ~خط ۳۱۷۵) هر متن «تلاش دوباره» در محیط مشتری را نویز/خطای آشکار می‌داند و تست رد شد؛ طبق همان قرارداد حذف شد. اگر مدل در دسترس نباشد، پاسخ از دادهٔ واقعی فروشگاه بی‌صدا می‌رسد.
- امضای «ساخته شده توسط m.azizi» حذف نشد (تست smoke حضورش را الزامی می‌داند).
- ستون «توانایی‌های دستیار» به چت برنگشت؛ چیپ‌های پیشنهاد همان نقش را دارند.

---

## ۳) تست‌های اجراشده (همه پس از آخرین تغییر)

| دستور | نتیجه |
|---|---|
| `npx tsc --noEmit` | بدون خطا |
| `npm run build` | موفق — `dist/index.html` ۹۲۲٫۴۸ کیلوبایت (gzip ۲۴۴٫۶۱) |
| `npm run smoke` | **۴۳۰ PASS، صفر FAIL** |
| `npm run verify:admin` | **۴۸ PASS** — All admin contract checks passed |
| `npm run verify:voice` | all checks passed |
| `npm run test:voice-cloud` | all cloud voice simulations passed |
| `npm run test:voice-android` | All Android TTS simulation scenarios passed |
| `npm run dev` + مرور مسیرها | `/` ، `/assistant` ، `/admin/dashboard` ، `/admin/login` ، `/products` → ۲۰۰ |

### سنجش‌های تازهٔ «فروش از داخل چت» (`assistant shop` — همه PASS)
کارت محصول دارد · قیمت از کاتالوگ واقعی · درگاه تأیید نمایش داده می‌شود · **پیش از تأیید چیزی به سبد نمی‌رود** ·
«نه» سبد را خالی نگه می‌دارد · بعد از «بله» پیام افزودن می‌آید · سبد (`localStorage`) دقیقاً یک قلم `jelly-strawberry` با تعداد ۱ ·
پیام «ثبت سفارش و پرداخت با خودتان است» · صفحه روی `/assistant` می‌ماند · تسویه حساب خودکار باز نمی‌شود · بدون واژهٔ فنی · بدون خطای زمان اجرا.

### سنجش‌های تازهٔ «ناموجودی» (`assistant out-of-stock` — همه PASS)
می‌گوید ناموجود است · هیچ پیشنهاد افزودنی نمی‌دهد · سبد خالی می‌ماند · برای محصولِ موجود، دکمهٔ افزودن فعال است · بدون واژهٔ فنی · بدون خطا.

### سنجش‌های قراردادی تازهٔ پنل (۹ مورد، در `verify-admin.mjs`)
هفت برچسب کارت بخش‌ها · `adm-card-action` (دکمهٔ گرد) · `.adm-card-grid` (سبک شبکه) · `Accordion` در صفحهٔ «ربات ژینو».
**این سنجش‌ها واقعاً اجرا می‌شوند**: با تغییر عمدیِ برچسب «ربات ژینو» به «ربات»، تست `FAIL: … is missing dashboard section card: ربات ژینو` داد و سپس فایل بازگردانده شد.

### محافظت‌هایی که هنوز پاس می‌شوند (نمونه)
چت: یک هدر، بدون فوتر/منوی فروشگاه، بدون پیل وضعیت، بدون واژهٔ فنی، RTL، کادر پیام با ورودی و دکمهٔ ارسال، ۴ چیپ خوشامد، جمع‌شدن پیشنهادها.
پنل: هر ۶ برچسب آماری، `?view=prices` و `?view=images`، نمایش سفارش‌ها و کنترل ترتیب/نمایش کارت‌ها.

---

## ۴) یک خطای من در میانهٔ کار (و رفع آن)

برای اثبات اینکه سنجش‌های تازهٔ پنل واقعاً کار می‌کنند، برچسب «ربات ژینو» را موقتاً عوض کردم و برای بازگرداندنش از
`git checkout src/admin/pages/AdminDashboardPage.tsx` استفاده کردم — چون آن فایل هنوز commit نشده بود، **کل بازنویسی داشبورد به نسخهٔ HEAD برگشت**.
بلافاصله همان فایل را با محتوای درست (۷ کارت، `listsOpen = true`) بازسازی کردم و همهٔ تست‌ها دوباره اجرا و پاس شدند
(`verify:admin` ۴۸ PASS، `smoke` ۴۳۰ PASS). از آن به بعد برای بازگرداندن تغییر موقت از ویرایش معکوس استفاده کردم، نه `git checkout`.

---

## ۵) مشکلات باقی‌مانده / نکته‌ها

1. **`retry()` بلااستفاده**: تابع تکرار آخرین پرسش در `useAssistantChat` هست ولی طبق قرارداد «بدون نویز تلاش دوباره» در UI استفاده نمی‌شود. یا حذفش کنم یا با تغییر همان تست، دکمهٔ ظریف برایش بگذارم — تصمیم با شما.
2. **دو میان‌بر «قیمت‌ها» و «تصاویر»** روی داشبورد ماندند چون `verify-admin.mjs` این دو مسیر را در همین فایل الزامی می‌داند. حذف کاملشان = تغییر آن تست (نیاز به تأیید شما).
3. **افزودن با تأیید در مسیر مدل هوش مصنوعی**: مدل دادهٔ ساختاری محصول نمی‌فرستد؛ کارت‌ها از `attachProducts()` (بر پایهٔ لینک‌های محصولِ خودِ پاسخ) ساخته می‌شوند. اگر مدل لینک ندهد، کارتی نمایش داده نمی‌شود (چت نمی‌شکند). برای افزودن مستقیم از پاسخِ مدل، باید در Edge Function یک `tool` تعریف شود — **تغییر سمت سرور؛ منتظر تأیید شما.**
4. **تنوع وزنی**: دکمهٔ «افزودن به سبد» ارزان‌ترین گزینهٔ قابل فروش را پیشنهاد می‌دهد (همان عددی که در متن پاسخ هست). انتخاب وزن دلخواه همچنان در صفحهٔ محصول است؛ اگر بخواهید، انتخابگر وزن کوچک به کارت چت اضافه می‌کنم.
5. **بازبینی چشمی**: سرور توسعه (`npm run dev`) روشن است تا `/assistant` و `/admin/dashboard` را خودتان ببینید. من فقط ۲۰۰ بودن مسیرها را بررسی کردم و اسکرین‌شات نگرفتم.
6. **Commit/Push**: انجام نشده — هر وقت بگویید، روی همین شاخه commit می‌کنم.

---

## ۶) جمع‌بندی محدودیت‌ها

- ظاهر سایت، محصولات، تصاویر، سبد خرید، Checkout و Routing: **بدون تغییر**.
- قابلیت‌ها و تنظیمات صدا: **کاملاً حفظ** (فایل‌های صوتی دست‌نخورده + هر سه تست صدا پاس).
- تغییر دیتابیسی: **انجام نشد**.
- تغییرات مرحله‌ای و محدود بود (۱۱ فایل کد/تست/مستند + ۲ فایل گزارش).
- Commit / Push / PR / Merge: **انجام نشد**.
