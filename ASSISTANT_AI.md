# ZHINO — دستیار ژینو (فاز ۵ تا ۸): محیط چت تمام‌صفحه + اتصال واقعی به دیتابیس و مدل

این سند، سند دستیار ژینو است: محیط چت تمام‌صفحهٔ `/assistant` چطور کار می‌کند،
اطلاعات واقعی از کجا می‌آید، چرا کلید API هیچ‌وقت در فرانت‌اند نیست، و
برای فعال‌سازی نهایی چه کارهایی لازم است.

---

## ۱. خلاصهٔ یک‌خطی

دکمهٔ شناور ربات، کاربر را به صفحهٔ مستقل `/assistant` می‌برد (پنجرهٔ Popup
حذف شده است). از فاز ۷ این صفحه یک **محیط چت خلوت و تقریباً تمام‌صفحه**
شبیه ChatGPT است: بدون هدر و فوتر فروشگاه، بدون منو، پرچم، ستون قابلیت‌ها
و بخش‌های اضافی؛ فقط یک سربرگ ساده (نشان ژینو، عنوان «دستیار ژینو»،
«گفتگوی تازه» و «بازگشت به فروشگاه») و خودِ گفتگو. از فاز ۸ این محیط
«بدون حرف فنی» هم شده است: هیچ متن «دیتابیس/موتور محلی/کاتالوگ/مدل
هوشمند» به مشتری نشان داده نمی‌شود، پیشنهادها چیپ‌های کوچک و جمع‌شونده‌اند،
پاسخ‌ها با صدای خودِ مرورگر خوانده می‌شوند و امضای کوچک سازنده زیر کادر
نوشتن نشسته است.
محیط گفتگو فارسی و RTL است، از **دیتابیس واقعی** فروشگاه
(محصولات، طعم‌ها، قیمت، موجودی، دستورها، تنظیمات ارسال) پاسخ می‌دهد، و برای
پاسخ‌های هوشمندتر از یک **پروکسی سمت سرور** (Supabase Edge Function یا
Backend خودتان) استفاده می‌کند — کلید مدل فقط در Secrets سرور می‌ماند و
هیچ‌وقت به مرورگر، Git یا بستهٔ عمومی نمی‌رسد.

دو «حقیقت» همچنان از هم جدا نگه داشته می‌شوند — اما از فاز ۸، هیچ‌کدام
در UI نیست؛ هر دو فقط در منطق داخلی (`useAssistantChat`) زنده‌اند:

| پرسش | پاسخ کجاست (فاز ۸) |
| --- | --- |
| داده از کجاست؟ (دیتابیس یا دادهٔ فروشگاه) | `chat.dataSource` — وضعیت اتصال و کارت اطلاعات؛ بدون هیچ متن روی صفحه |
| پاسخ از کجاست؟ (مدل هوشمند یا موتور محلی) | `chat.connection` + شمارندهٔ خطای مدل؛ بدون هیچ متن روی صفحه |

یعنی اتصال دیتابیس، Fallback محلی و تصمیم «پرسش به مدل برود یا نه»
دقیقاً مثل قبل کار می‌کنند — فقط بی‌صدا. تنها چیزی که مشتری می‌بیند،
خودِ پاسخ است (و در یک حالت، یک یادداشت کوتاه و انسانی؛ پایین‌تر).

---

## ۲. معماری (سه لایهٔ مستقل)

```
src/pages/AssistantPage.tsx            ← پوستهٔ محیط چت /assistant (سربرگ ساده + تمام‌قد)
src/components/assistant/
  AssistantChat.tsx                    ← گفتگو: پیام خوشامد، حباب‌ها، کادر نوشتن ChatGPT-وار
  useAssistantChat.ts                  ← منطق گفتگو: انتخاب منبع پاسخ
  ZhinoAssistant.tsx                   ← دکمهٔ شناور (Link به /assistant)
  AssistantAvatar.tsx / assistantData.ts / assistant.css

src/services/assistant/
  types.ts                             ← قرارداد مشترک داده
  knowledge.ts                         ← کارت اطلاعات واقعی فروشگاه (grounding)
  engine.ts                            ← موتور پاسخ محلی (آفلاین، بدون شبکه)
  client.ts                            ← اتصال به مدل از پشت سرور (بدون کلید در مرورگر)

supabase/functions/zhino-assistant/    ← پروکسی امن مدل (Deno Edge Function)
  index.ts + README.md
```

جریان هر پرسش:

1. کاربر پیام می‌نویسد (یا چیپ پیشنهادی را می‌زند).
2. `useAssistantChat` از `knowledge.ts` یک **کارت اطلاعات زنده** می‌گیرد
   (محصولات، بسته‌ها، قیمت‌ها، موجودی، آستانهٔ ارسال رایگان، دستورهای رسمی).
   این کارت از همان سرویس‌های فروشگاه ساخته می‌شود؛ اگر Supabase پیکربندی
   شده باشد، همان snapshot دیتابیس است (`dataSource: 'database'`).
3. اگر مدل پیکربندی شده باشد، پرسش + کاتالوگ فشرده به Edge Function/Backend
   می‌رود. **تابع خودش هم دیتابیس را می‌خواند** و اگر موفق شود، همان دادهٔ
   دیتابیس را جای کاتالوگ فرستاده‌شده به مدل می‌دهد (`knowledge: 'database'`).
4. اگر پیکربندی نشده باشد، مدل پاسخ ندهد یا تایم‌اوت شود، `engine.ts` همان
   لحظه پاسخ را از دادهٔ واقعی می‌سازد (`source: 'local'`) و زیر پیام هم
   شفاف می‌نویسد که پاسخ از اطلاعات فروشگاه آمده است.

---

## ۲٫۱ رابط کاربری فاز ۷ و ۸ — محیط چت شبیه ChatGPT

هدف: کاربر با زدن دکمهٔ دستیار، مستقیم وارد یک محیط گفتگوی خلوت، مدرن و
تمام‌قد شود — بدون هیچ بخش اضافی سایت.

| قبل (فاز ۵/۶) | حالا (فاز ۷ و ۸) |
| --- | --- |
| قالب فروشگاه: هدر ثابت + فوتر + منو + پرچم‌ها روی صفحهٔ دستیار | صفحهٔ دستیار بدون هدر و فوتر فروشگاه رندر می‌شود (`Layout`) |
| سربرگ بزرگ Burgundy با مدال ربات، شعار و دکمهٔ «بازگشت به سایت» | سربرگ باریک و Premium: نشان کوچک ژینو، عنوان «دستیار ژینو»، «گفتگوی تازه» و «بازگشت به فروشگاه» (پیل وضعیت در فاز ۸ حذف شد) |
| ستون کنار: «توانایی‌های دستیار» + «شفاف و بی‌ادعا» + لینک‌های مفید | حذف شد؛ محیط تک‌ستونه و خلوت |
| پیام خوشامد طولانی داخل حباب اول | پیام خوشامد کوتاه و فارسی، وسط صفحه، با پیشنهادهای آماده زیرش |
| کادر ورود پیام قرینه با راهنمای بلند | کادر نوشتن پایین صفحه: گوشه‌های نرم، سایه و border ظریف، دکمهٔ ارسال داخل کادر، hint کوتاه |

نکات پیاده‌سازی:

- صفحه دقیقاً به اندازهٔ دید کاربر است (`100dvh`؛ با `100vh` برای مرورگرهای
  قدیمی‌تر) و فقط فهرست پیام‌ها اسکرول می‌شود؛ کادر نوشتن همیشه پایین
  می‌ماند و فاصلهٔ ایمن پایین موبایل (`env(safe-area-inset-bottom)`) رعایت
  می‌شود.
- ستون گفتگو وسط‌چین و باریک است (حداکثر `48rem`)، دقیقاً مثل چت‌های مدرن.
- رنگ‌بندی از توکن‌های برند می‌آید: زرشکی اصلی `#71313B` (حباب کاربر، دکمهٔ
  ارسال، لینک‌های پاسخ) و رنگ Hero `#5A1725` (نام برند و عنوان خوشامد) روی
  زمینهٔ روشن ivory با هالهٔ بسیار ملایم زرشکی/طلایی — روشن و لوکس، بدون
  سفیدشدگی زننده.
- «گفتگوی تازه» (همان `clear` قبلی) به حالت خوشامد اولیه برمی‌گردد؛ پس
  رفتارش برای کاربر قابل پیش‌بینی است.
- موبایل: عنوان برند جمع می‌شود، دکمهٔ بازگشت آیکنی می‌شود، چیپ‌های
  پیشنهاد `flex-wrap` دارند و `max-width: 100%` (هیچ‌وقت از صفحه بیرون
  نمی‌زنند؛ `white-space: nowrap` عملاً ممنوع است)، و اندازهٔ فونت کادر
  ورودی `1rem` می‌ماند تا iOS صفحه را زوم نکند.

**هیچ قابلیت فنی‌ای حذف نشد:** منطق گفتگو (`useAssistantChat`)، موتور محلی
(`engine.ts`)، کارت اطلاعات (`knowledge.ts`)، اتصال به مدل (`client.ts`)،
Edge Function، RLS، سبد خرید، مسیرهای سایت و پنل مدیریت همه دست‌نخورده‌اند.
تنها لایهٔ نمایش (صفحه + محیط گفتگو + CSS) بازطراحی شده است.

---

## ۳. دو مسیر اتصال (فقط یکی را لازم دارید)

### مسیر الف — Supabase Edge Function (پیشنهاد اصلی)

```bash
supabase secrets set AI_API_KEY="<کلید مدل>" AI_MODEL="<نام مدل>"
supabase functions deploy zhino-assistant
```

- اگر `VITE_SUPABASE_URL` و کلید عمومی پروژه تنظیم باشند، فروشگاه خودش این
  تابع را روی `<VITE_SUPABASE_URL>/functions/v1/zhino-assistant` صدا می‌زند.
- `AI_API_URL` اختیاری است (هر سرویس سازگار با OpenAI).
- تابع برای «کارت اطلاعات» خودش این جدول‌ها را **فقط-خواندنی** می‌خواند:
  `products`, `product_variants`, `inventory`, `recipes`, `site_settings` —
  با کلید عمومی پروژه (anon)، پس RLS دقیقاً همان سقف بازدیدکننده را اعمال
  می‌کند. هیچ‌وقت `orders`/`order_items` و هیچ‌وقت `service_role`.
- `SUPABASE_URL` و `SUPABASE_ANON_KEY` خودکار در محیط تابع هستند.
- جزئیات کامل: `supabase/functions/zhino-assistant/README.md`.

### مسیر ب — Backend اختصاصی

```bash
# .env.local  (فقط آدرس — نه کلید)
VITE_ASSISTANT_API_URL=https://api.example.com/assistant/chat
```

قرارداد: `POST` با بدنهٔ زیر و پاسخ `{ "reply": "…" }`.

```json
{
  "message": "قیمت ژله انار چند است؟",
  "history": [{ "role": "user", "content": "…" }],
  "catalog": "متن فشردهٔ محصولات/قیمت/موجودی/دستورها",
  "catalogSource": "database",
  "locale": "fa-IR"
}
```

پاسخ می‌تواند `knowledge: database | client | none` هم داشته باشد؛ اگر
`none` بود، زیر پاسخ یک خط کوتاه و انسانی می‌آید که «این پاسخ از فهرست
فروشگاه نبود، دربارهٔ قیمت و موجودی دوباره بپرسید» — بدون هیچ واژهٔ فنی.
اگر Backend شما خودش دیتابیس را می‌خواند، `catalog` را نادیده بگیرید و همان
جدول‌های بالا را با کلید عمومی بخوانید.

### حالت ج — بدون هیچ پیکربندی

دستیار کاملاً محلی کار می‌کند: **صفر درخواست شبکه‌ای**، همهٔ پاسخ‌ها از
کاتالوگ، تنظیمات و دستورهای واقعی فروشگاه. از فاز ۸ هیچ نوار وضعیت و هیچ
یادداشت فنی‌ای نمایش داده نمی‌شود: گفتگو ساده و کامل کار می‌کند.

---

## ۴. چرا کلید API در فرانت‌اند نیست؟

- فرانت‌اند فقط **آدرس** Endpoint را می‌شناسد (`VITE_ASSISTANT_API_URL`) و
  هدر `Authorization` فقط «کلید عمومی» پروژه (publishable/anon) است — همان
  کلیدی که Row Level Security آن را محدود می‌کند؛ هیچ راز صادرکنندهٔ اعتبار
  در بستهٔ نهایی نیست.
- کلید مدل در `AI_API_KEY` (Secrets سوپابیس) یا محیط سرور شما می‌ماند.
- خروجی تابع هیچ‌وقت کلید یا خطای خام سرویس‌دهنده را برنمی‌گرداند.
- `scripts/verify-supabase.mjs` (هم در CI) فایل‌های مخزن را برای الگوهای
  کلید (`sk-…`, `AIza…`, `sb_secret_…`, JWT سرویس‌رول و…) اسکن می‌کند و
  ساختار Edge Function را هم بدون هیچ اتصال شبکه‌ای بررسی می‌کند.
- `.env.example` فقط متغیرهای عمومی را مستند می‌کند و صریح می‌گوید که هر
  متغیر `VITE_*` در بستهٔ مرورگر قرار می‌گیرد.

---

## ۵. «زمین‌گیری» پاسخ‌ها روی دادهٔ واقعی

- `knowledge.ts` تنها منبع حقیقت است: `getVisibleProducts()` (کاتالوگ همان
  فروشگاه — سوپابیس یا دادهٔ پایه + تغییرات پنل)، `getSettings()` (آستانهٔ
  ارسال رایگان و کرایه‌ها) و `getActiveRecipes()` (دستور رسمی روی بسته،
  قابل ویرایش در پنل).
- اگر Supabase پیکربندی شده باشد، همین سرویس‌ها از snapshot دیتابیس
  می‌خوانند؛ پس قیمت و موجودی که دستیار می‌گوید عیناً همان چیزی است که در
  صفحهٔ محصول و سبد خرید دیده می‌شود. `dataSource` این را در UI اعلام می‌کند.
- Edge Function هم مستقل از مرورگر همان جدول‌ها را می‌خواند (RLS)، تا حتی
  وقتی مرورگر کاتالوگ کهنه‌ای دارد، مدل دادهٔ تازه بگیرد.
- قیمت/موجودی هیچ‌جا هاردکد نشده؛ در لحظهٔ هر پاسخ خوانده می‌شود.
- مدل هم کاتالوگ را همراه پرسش می‌گیرد و در System Prompt موظف است فقط از
  همان بگوید؛ اگر عددی در آن نبود، باید بگوید «در فهرست فروشگاه نمی‌بینم»
  و از خودش عدد نسازد.
- موتور محلی هم همین قاعده را دارد: طعم/محصول ناموجود → پاسخ صریح «در
  فهرست فعلی نیست»، بدون ساختن محصول یا قیمت.
- پرسش‌های بیرون از حوزهٔ ژله/کاستر/خرید با احترام و شفافیت رد می‌شوند.

---

## ۶. قابلیت‌های امروز و نقشهٔ راه

آمادهٔ استفاده در همین فاز (هم آفلاین، هم پشت مدل):

| قابلیت | نمونهٔ پرسش |
| --- | --- |
| راهنمای انتخاب محصول | «برای انتخاب محصول راهنمایی می‌کنید؟» |
| فهرست طعم‌ها | «چه طعم‌هایی دارید؟» |
| منبع اطلاعات | «اطلاعاتت را از کجا می‌آوری؟» |
| دستور مرحله‌به‌مرحله | «طرز تهیه کاستر چطور است؟» |
| قیمت و موجودی واقعی | «قیمت ژله توت فرنگی چند است؟» |
| پیشنهاد دسر شخصی‌سازی‌شده | «یک دسر سرد و میوه‌ای پیشنهاد بده» |
| پیشنهاد بر اساس بودجه | «با ۳۰۰ هزار تومان چه ترکیبی بگیرم؟» |
| ترکیب طعم‌ها | «ژله انار با چه طعمی خوب می‌شود؟» |
| محاسبهٔ مقدار برای تعداد نفرات | «برای ۱۰ نفر چند بسته لازم است؟» |
| ارسال و کرایه | «هزینه ارسال چقدر است؟» |
| راهنمای سفارش | «چطور سفارش ثبت کنم؟» |
| افزودن به سبد خرید **با تأیید مشتری** | «ژله توت فرنگی را به سبد خرید اضافه کن» |

گام بعدی (ساختار از همین حالا آماده است): پرسش‌های چندنوبتی برای سلیقه و
مناسبت، پیشنهاد بر پایهٔ سفارش‌های قبلی کاربر، و ابزارهای function-calling
روی همان توابع `knowledge.ts` (کارت محصول و افزودن با تأیید انجام شده‌اند —
بخش ۶٫۰۲).

## ۶٫۰۲ فروش از داخل گفتگو — کارت محصول و افزودن با تأیید

پاسخ‌هایی که محصول معرفی می‌کنند (`price-stock`، `flavor`، `catalog`،
`suggestion`، `budget`، `pairing` و قابلیت `add-to-cart`) یک
`products: AssistantProductFact[]` همراه دارند؛ رابط کاربری از همان داده
**کارت محصول** می‌سازد (نام، قیمت، وزن، وضعیت موجودی). هیچ عدد تازه‌ای
ساخته نمی‌شود و هیچ درخواست شبکه‌ای هم زده نمی‌شود — همان
`buildAssistantContext()` که متن پاسخ از آن ساخته شده.

قاعدهٔ قطعی افزودن به سبد:

1. دکمهٔ «افزودن به سبد» روی کارت (`.zhino-assistant-add`) فقط یک
   **پیشنهاد** می‌سازد (`AssistantCartOffer`)؛ هیچ چیز به سبد نمی‌رود.
2. درگاه تأیید (`.zhino-assistant-cart-confirm`) با «بله، اضافه کن» و «نه»
   نمایش داده می‌شود. «نه» → `dismissCartOffer` و سبد دست‌نخورده.
3. «بله» → `acceptCartOffer` در `useAssistantChat.ts` که قیمت و موجودی را
   **دوباره از کاتالوگ** می‌خواند و سپس همان مسیر همیشگی فروشگاه
   (`CartContext.addItemWithToast`) را صدا می‌زند.
4. **هیچ Checkout یا پرداخت خودکاری اجرا نمی‌شود**؛ بعد از افزودن فقط یک
   لینک «دیدن سبد خرید» و یک جملهٔ صریح («ثبت سفارش و پرداخت با خودتان
   است») نشان داده می‌شود.

در مسیر مدل هوش مصنوعی، مدل دادهٔ ساختاری محصول نمی‌فرستد؛ کارت‌ها از
`attachProducts()` در `useAssistantChat.ts` و بر پایهٔ لینک‌های محصولِ خودِ
پاسخ (و در نبودِ آن‌ها، جست‌وجوی متن پرسش) ساخته می‌شوند.

تست: سوئیت `assistant shop` در `scripts/smoke.mjs` — کارت محصول، قیمت واقعی،
درگاه تأیید، خالی‌ماندن سبد پیش از تأیید، «نه»، افزودن با «بله»
(یک قلم در `localStorage`)، نبودِ صفحهٔ تسویه و نبودِ هر واژهٔ فنی.

## ۶٫۰۱ پاسخ صوتی (فاز ۸)

پاسخ‌ها با **Text-to-Speech خودِ مرورگر** خوانده می‌شوند
(`src/components/assistant/useAssistantSpeech.ts`):

- **آزارنده نیست:** پیش‌فرض خاموش است، یک دکمهٔ کوچک در کادر نوشتن آن را
  روشن می‌کند، ترجیح کاربر در `localStorage` (کلید
  `zhino_assistant_voice_v1`) می‌ماند و دکمهٔ «توقف خواندن» همیشه در دسترس
  است (زیر کادر و «خواندن پاسخ» زیر هر پیام).
- **خصوصی:** فقط *متن پاسخ دستیار* خوانده می‌شود — هرگز سؤال مشتری، نام،
  نشانی یا سبد خرید. هیچ درخواست شبکه‌ای زده نمی‌شود، هیچ سرویس صوتی خارجی
  صدا نمی‌شود و میکروفن هرگز باز نمی‌شود.
- **فارسی، و اگر نبود:** صدای `fa` انتخاب می‌شود؛ اگر نبود صدای `ar`
  (خط فارسی را تا حد خوبی می‌خواند) و اگر هیچ‌کدام نبود، خواندن شروع
  نمی‌شود و یک خط کوتاه گفته می‌شود. نبودِ `speechSynthesis` (مرورگر
  قدیمی، iOS Safari در برخی حالت‌ها، jsdom) هم گفتگو را نمی‌شکند: متن،
  همان‌طور که بود، نمایش داده می‌شود.
- متن بلند به تکه‌های ~۱۸۰ نویسه‌ای شکسته می‌شود (قطع‌شدن Safari/Chrome در
  جملات طولانی) و با ترک صفحه یا «گفتگوی تازه» خواندن بی‌درنگ ساکت می‌شود.

## ۶٫۱ مدیریت خطا، تایم‌اوت و سهمیه

| وضعیت | رفتار (فاز ۸) |
| --- | --- |
| Endpoint پیکربندی نشده | پاسخ محلی، بی‌صدا — هیچ بنر یا یادداشت فنی نیست |
| خطای شبکه/سرور/تایم‌اوت (۱۲ ثانیه) | پاسخ محلی از همان دادهٔ واقعی؛ برای مشتری هیچ تغییری نمی‌کند |
| دو خطای پشت‌سرهم | دیگر برای هر پیام منتظر Endpoint خراب نمی‌مانیم؛ پاسخ محلی فوری |
| سهمیهٔ ۴۲۹ | پاسخ محلی، بدون پیام «سهمیه» به مشتری |
| پاسخ نامعتبر از سرور | پاسخ محلی، بدون شکستن گفتگو |
| مدل بدون دادهٔ فروشگاه (`knowledge: none`) | یک خط کوتاه و انسانی زیر پاسخ (بدون واژهٔ فنی) |

«تلاش دوباره» و نوار «پاسخ محلی داده شد» در فاز ۸ حذف شدند: هیچ‌کدام
برای مشتری معنایی نداشتند و Fallback بی‌صدا کار می‌کند. منطق شمارندهٔ خطا
(`MAX_REMOTE_FAILURES`) و `AssistantRemoteError.kind` همچنان سر جایشان‌اند.

سقف‌های سمت سرور: پیام ۶۰۰ نویسه (۱۳ نویسه کم‌تر از سقف کلاینت)،
بدنه ۲۴KB، کاتالوگ ۸۰۰۰ نویسه، تاریخچه ۸ نوبت، پاسخ ۴۰۰۰ نویسه،
۲۰ پرسش در ۵ دقیقه برای هر IP (در هر نمونه).

---

## ۷. تست‌ها (فاز ۶ تا ۸)

`npm run smoke` (بدون شبکه، با jsdom) این‌ها را تضمین می‌کند:

- `/assistant` مستقل رندر می‌شود، دکمهٔ شناور روی آن تکرار نمی‌شود و Popup
  وجود ندارد.
- **محیط چت فاز ۷:** صفحه فقط یک `header` دارد (سربرگ چت، بدون منوی سایت)،
  هیچ `footer`، هیچ لینک خانه/محصولات/تماس/سبد خرید و هیچ ستون «توانایی‌های
  دستیار» رندر نمی‌شود؛ پیام خوشامد فارسی کوتاه دیده می‌شود؛ و کادر نوشتن
  (فرم با ورودی `#zhino-assistant-input` و دکمهٔ «ارسال پیام») سر جایش است.
- دکمهٔ شناور یک `<a href="…/assistant">` واقعی است و کلیکش صفحه را باز
  می‌کند؛ URL هم `/assistant` می‌شود.
- «بازگشت به فروشگاه» به صفحهٔ اصلی می‌رود و دکمهٔ Back مرورگر به صفحهٔ
  دستیار برمی‌گردد.
- گفتگو در حالت آفلاین: نشان «در حال پاسخ‌گویی»، قیمت واقعی کاتالوگ، دستور
  رسمی ژله، پیشنهاد بودجه از قیمت‌های واقعی، و پاسخ محترمانه به پرسش
  بی‌ربط.
- **قرارداد فاز ۸ (هیچ حرف فنی به مشتری):** نه در متن، نه در `title` و
  نه در هیچ attribute دیگری واژهٔ «دیتابیس/موتور محلی/کاتالوگ/همگام‌سازی/
  سوپابیس/مدل هوشمند» پیدا نمی‌شود؛ پیل وضعیت و بنر خطای اتصال اصلاً
  رندر نمی‌شوند؛ و در بستهٔ نهایی (`dist/index.html`) هم هیچ‌یک از جمله‌های
  فنی قدیمی (`بدون مدل هوشمند`، `ارتباط با دستیار هوشمند برقرار نشد`، …)
  وجود ندارد.
- **پیشنهادها:** در حالت خوشامد چیپ‌های وسط‌چین زیر پیام خوشامد، و بعد از
  شروع گفتگو یک ردیف جمع‌شونده (`aria-expanded`) که بازشدنش چیپ‌ها را
  نشان می‌دهد و کلیک روی هر چیپ همان پرسش را می‌فرستد.
- **صدا (با یک Web Speech API ساختگی در jsdom):** پیش‌فرض خاموش، روشن/خاموش
  شدن و ذخیرهٔ ترجیح، خوانده‌شدن *فقط* متن پاسخ (سؤال مشتری هرگز به موتور
  صوتی نمی‌رود)، برچسب زبانی `fa`، کارکرد دکمهٔ توقف، صفر بودن درخواست‌های
  شبکه برای صدا، و بی‌ضرر بودن کامل وقتی مرورگر `speechSynthesis` یا صدای
  فارسی ندارد.
- **CSS موبایل:** چیپ‌ها `flex-wrap` و `max-width: 100%` دارند (بدون
  `white-space: nowrap`)، دکمهٔ صدا ۲٫۳۵rem است، امضا بسیار ریز و کم‌رنگ
  است، و هیچ قانونی برای پیل وضعیت/بنر اتصال/یادداشت فنی در stylesheet
  نمانده است.
- مسیر مدل (mock): پرسش به Endpoint پیکربندی‌شده می‌رود، بدنهٔ درخواست فقط
  `message/history/catalog/catalogSource/locale` است، کاتالوگ واقعی همراه
  می‌رود، و هیچ کلید/رازی در درخواست مرورگر نیست.
- **دیتابیس واقعی:** با یک Supabase شبیه‌سازی‌شده، پاسخ قیمت از ردیف دیتابیس
  (۱۸۷٬۰۰۰ به‌جای ۲۰۰٬۰۰۰ پایه)، موجودی، آستانهٔ ارسال رایگان از
  `site_settings` و متن دستور از `recipes` خوانده می‌شود.
- **Edge Function واقعی:** خودِ `index.ts` با یک `Deno` و شبکهٔ mock اجرا
  می‌شود: پرامپت مدل باید قیمت/موجودی/دستور/تنظیمات دیتابیس را داشته باشد،
  خواندن‌ها فقط با کلید anon و فقط از جدول‌های عمومی، ۵۰۱ بدون Secret،
  ۵۰۲ در خطای مدل، ۴۱۳ برای بدنهٔ بزرگ، ۴۰۳ برای دامنهٔ ناشناس، ۴۲۹ برای
  سهمیهٔ پرسش، و هیچ کلیدی در هیچ پاسخی.
- **فارسی محاوره‌ای:** «چنده»، «۵۰۰ تومن»، «چقدر پودر لازمه»، «چه طعم هایی
  دارید»، «موجوده»، «هزینه پست»، «لایه لایه»، «سریع و راحت»، «شعر بگو».
- **حفظ مسیرها:** بعد از `/assistant`، صفحه‌های `/products`، `/products/:id`،
  `/cart` (با سبد ذخیره‌شده) و `/admin/login` دقیقاً مثل قبل رندر می‌شوند.

`npm run verify:supabase` هم بدون هیچ اتصال شبکه‌ای بررسی می‌کند که تابع
Edge فقط جدول‌های عمومی را می‌خواند، به `orders`/`service_role` دست نمی‌زند،
سقف‌های ورودی را دارد، و لایهٔ فرانت‌اند دستیار هیچ ماژول سفارش/کلید سری‌ای
را import نمی‌کند و هیچ قیمتی را هاردکد نکرده است.

اجرای همه: `npm run typecheck && npm run build && npm run smoke`
(در CI: `typecheck` → `verify:supabase` → `verify:data` → `build` → `smoke`).
