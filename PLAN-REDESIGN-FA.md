# طرح بازطراحی صفحهٔ چت ژینو و پنل مدیریت (گزارش قبل از اجرا)

> وضعیت: **فقط گزارش و طرح** — هیچ فایلی از `src/` تغییر نکرده، هیچ commit/push/PR انجام نشده.
> تاریخ بررسی: ۲۰۲۶/۰۹/۱۸ — شاخهٔ کاری `arena/01a0b516-zheno-website` (بدون تغییر نسبت به `cb90b80`).

---

## ۱) وضعیت فعلی کد (چیزی که الان هست)

### ۱‑۱) صفحهٔ چت `/assistant`
| فایل | نقش | نکته |
|---|---|---|
| `src/pages/AssistantPage.tsx` | پوستهٔ صفحه | سربرگ (نشان + «Zhino / دستیار ژینو» + گفتگوی تازه + بازگشت به فروشگاه) و سپس `<AssistantChat/>` |
| `src/components/assistant/AssistantChat.tsx` (~۲۸KB) | کل رابط چت | حالت خوشامد وسط صفحه، حباب‌ها، چیپ‌ها، کادر پیام، دکمهٔ صدا، کنترل پخش، امضا |
| `src/components/assistant/useAssistantChat.ts` | منطق گفتگو | انتخاب بین مدل سرور (`client.ts`) و موتور محلی (`engine.ts`)، Fallback بی‌صدا |
| `src/components/assistant/useAssistantSpeech.ts` (~۳۹KB) | صدا | صدای ابری + صدای مرورگر، مکث/ادامه/توقف، خطاها |
| `src/components/assistant/assistant.css` (~۳۶KB) | ظاهر چت | همهٔ کلاس‌های `zhino-assistant-*` |
| `src/components/assistant/AssistantAvatar.tsx` | آواتار | تصویر اختصاصی + جایگزین برداری |

**مهم:** بیشترِ خواسته‌های شما از فازهای قبل تا حد زیادی انجام شده است:
- هدر خلوت ✅ — پیل وضعیت فنی حذف شده (`smoke.mjs` تست می‌کند که `.zhino-assistant-status` وجود نداشته باشد).
- متن‌های فنی از چت حذف شده ✅ — تست `expectNoInternalWording` روی کلمه‌هایی مثل «دیتابیس»، «کاتالوگ»، «هوش مصنوعی».
- چیپ‌های پیشنهاد ✅ — وسط صفحه در حالت خوشامد، بعد از شروع گفتگو در ردیف جمع‌شونده.
- کادر پیام ثابت پایین ✅، حباب تفکیک‌شده کاربر/ربات ✅، حالت «در حال پاسخ‌گویی» ✅.

**چیزی که هنوز با خواستهٔ شما فاصله دارد:**
1. **کنترل‌های صدا شلوغ‌اند**: زیر *هر* پاسخ ربات یک دکمهٔ متنی «خواندن پاسخ» هست، و هم‌زمان پایین کادر پیام هم «مکث/توقف» و نوار صدا نمایش داده می‌شود → دو جا برای یک کار.
2. **قابلیت‌های فروشگاهی برای کاربر روشن نیست** (طبق خواستهٔ شما). فهرست `ASSISTANT_CAPABILITIES` در `engine.ts` از فاز ۷ از UI حذف شده و هیچ‌جا به مشتری گفته نمی‌شود ربات چه کارهایی می‌کند (جست‌وجو، قیمت/موجودی، پیشنهاد طعم، طرز تهیه).
3. **افزودن به سبد از داخل چت وجود ندارد**: الان ربات فقط *لینک* «سبد خرید» یا «مشاهدهٔ محصولات» می‌دهد (`engine.ts` خطوط ۷۲۶، ۸۰۷، ۹۰۵، ۹۲۴، ۹۴۱). هیچ `addItem` از چت صدا زده نمی‌شود.
4. **کارت محصول در چت نیست**: قیمت/موجودی فقط به شکل متن `• ژله توت فرنگی — ۲۵۰ گرم: ۲۰۰٬۰۰۰ تومان — موجود` چاپ می‌شود (`priceStockReply`).
5. **حالت خطای سادهٔ کاربر**: خطاها بی‌صدا به موتور محلی می‌افتند (خوب است) ولی «تلاش دوباره» وجود ندارد.
6. امضای «ساخته شده توسط m.azizi» زیر کادر است (تست smoke آن را لازم می‌داند → **حذف نمی‌شود**، فقط ظریف‌تر).

### ۱‑۲) پنل مدیریت
| فایل | نقش |
|---|---|
| `src/admin/AdminLayout.tsx` | سایدبار دسکتاپ + نوار دکمه‌های گرد موبایل (`ADMIN_NAV`) |
| `src/admin/nav.ts` | ۸ آیتم ناوبری: داشبورد، ربات ژینو، محصولات، سفارش‌ها، موجودی، دستور تهیه، محتوای سایت، تنظیمات |
| `src/admin/pages/AdminDashboardPage.tsx` | صفحهٔ اصلی: هدر برند + نوار وضعیت سفارش‌ها + **۹ کارت «دسترسی سریع»** + آمار تکمیلی + آخرین سفارش‌ها + کم‌موجودی‌ها |
| `src/admin/pages/AdminAssistantPage.tsx` | «ربات ژینو» — صفحهٔ **مستقل** با **۳ بخش جمع‌شونده** (صدا / رفتار ربات / وضعیت سرویس) |
| `src/admin/admin.css` | کلاس‌های `adm-*` |

**مهم:** دو خواستهٔ اصلی شما از قبل وجود دارد:
- «ربات ژینو» صفحهٔ مستقل دارد ✅ (مسیر `/admin/assistant`).
- تنظیماتش در آکاردئون‌های جمع‌شونده است ✅ (نه همهٔ دکمه‌ها هم‌زمان).

**چیزی که شلوغ است و باید خلوت شود:**
- صفحهٔ اصلی پنل **۹ کارت دسترسی سریع** دارد (داشبورد، سفارش‌ها، محصولات، موجودی، قیمت‌ها، تصاویر، محتوای سایت، دستور تهیه، تنظیمات) — شما **۴ کارت** می‌خواهید: محصولات / سفارش‌ها / ربات ژینو / تنظیمات.
- نوار وضعیت + آمار تکمیلی + آخرین سفارش‌ها + کم‌موجودی‌ها + بخش «تنظیم کارت‌ها» همگی روی یک صفحه‌اند.
- کارت‌ها الان فقط آیکون + عنوان + توضیح‌اند؛ **دکمهٔ گرد** ندارند (آیکون داخل دایره هست، ولی خودش دکمه نیست).

### ۱‑۳) قید فنی مهم (تست قرارداد پنل)
`scripts/verify-admin.mjs` روی `AdminDashboardPage.tsx` این‌ها را **الزامی** چک می‌کند:
- ۶ برچسب آماری: `سفارش‌های جدید`، `در حال آماده‌سازی`، `سفارش‌های ارسال‌شده`، `سفارش‌های تکمیل‌شده`، `مجموع فروش`، `موجودی کم یا رو به اتمام`
- دو مسیر مستقیم: `/admin/products?view=prices` و `/admin/products?view=images`

→ یعنی «قیمت‌ها» و «تصاویر محصولات» نمی‌توانند کامل از داشبورد حذف شوند؛ در طرح من به‌شکل **دو لینک ظریف داخل کارت «محصولات»** نگه داشته می‌شوند (هم قرارداد تست می‌ماند، هم صفحه خلوت می‌شود).

### ۱‑۴) خط پایهٔ تست‌ها (الان، قبل از تغییر)
```
npx tsc --noEmit        → بدون خطا
npm run verify:admin    → All admin contract checks passed
npm run build           → dist/index.html 904.77 kB (gzip 241.20 kB)
npm run smoke           → All smoke checks passed
```
این چهار دستور، معیار «خراب نشد» در هر مرحله‌اند.

### ۱‑۵) پالت برند (که تغییر نمی‌کند)
`--color-wine-950 #2a0b12` · `wine-900 #5a1725` · `wine-800 #71313b` (زرشکی اصلی) ·
`cream-50 #faf7f2` (سفید/شیری) · `cream-100 #f3ede4` · `gold-400 #c9ae7c` ·
متن `espresso #292321` و `mocha #6e5f57` — فونت Vazirmatn / Marcellus.

---

## ۲) طرح پیشنهادی (مرحله‌ای و محدود)

### مرحلهٔ ۱ — داشبورد پنل: ۷ کارت مستقل و هم‌سطح ✅ (طبق تصمیم شما)
**فایل‌ها:** `src/admin/pages/AdminDashboardPage.tsx` · `src/admin/admin.css` (کلاس‌های تازهٔ `adm-card*`)

- صفحهٔ اصلی: هدر برند کوچک + نوار ظریف وضعیت سفارش‌ها + **۷ کارت مستقل، هم‌سطح و کنار هم** (بدون هیچ زیرمجموعه‌ای):
  محصولات · سفارش‌ها · ربات ژینو · تنظیمات · موجودی · دستور تهیه · محتوای سایت
- هر کارت: آیکون در کاشی نرم + عنوان کوتاه + یک خط توضیح + **دکمهٔ گرد** (فلش) در گوشه.
- گوشه‌های نرم (`rounded-2xl`)، سایهٔ بسیار ظریف، هاور با حلقهٔ زرشکی کم‌رنگ — بدون گرادیان سنگین.
- شبکه: ۳ ستون در دسکتاپ، ۲ در تبلت، ۱ در موبایل — همهٔ کارت‌ها هم‌اندازه و هم‌سبک.
- آمارها (سفارش جدید / فروش / کم‌موجودی) در همان نوار ظریف بالا می‌مانند (برچسب‌های لازم برای `verify-admin` حفظ می‌شوند).
- بخش «تنظیم کارت‌ها» به یک بخش جمع‌شوندهٔ کوچک و خاموش منتقل می‌شود (منطق `dashboardPreferences.ts` دست‌نخورده).
- «قیمت‌ها» و «تصاویر محصولات» به‌شکل **یک ردیف لینک ظریف و جدا** زیر کارت‌ها می‌مانند (نه زیرمجموعهٔ کارت) — چون تست قرارداد `verify-admin.mjs` این دو مسیر را در همین فایل لازم می‌داند.
- **Routing تغییر نمی‌کند**: هیچ مسیر `/admin/*` حذف یا جابه‌جا نمی‌شود.

### مرحلهٔ ۲ — صفحهٔ «ربات ژینو» (فقط ظاهر)
**فایل:** `src/admin/pages/AdminAssistantPage.tsx` (+ کمی `admin.css`)
- همان ۳ دستهٔ جمع‌شونده، با کاشی آیکون، عنوان روشن‌تر، توضیح کوتاه‌تر و فاصلهٔ بیشتر.
- دکمهٔ «ذخیره» و «بازنشانی» در یک نوار ثابت و جمع‌وجور.
- **هیچ قابلیتی حذف نمی‌شود**: کلید اصلی صدا، خواندن خودکار، صدای ابری، انتخاب صدا، سرعت، تست صدا، پیشنهادهای شروع، لحن، بررسی وضعیت سرویس — همه سرِ جایشان.

### مرحلهٔ ۳ — پولیش صفحهٔ چت (فقط CSS + ترتیب عناصر)
**فایل‌ها:** `src/components/assistant/assistant.css` · `AssistantChat.tsx` · `AssistantPage.tsx`
- سربرگ: کم‌ارتفاع‌تر، یک خط جداکنندهٔ خیلی روشن، دکمه‌های گرد یک‌اندازه.
- حباب‌ها: گوشهٔ نرم‌تر، سایهٔ `0 1px 2px` بسیار ظریف، حباب کاربر زرشکی ملایم با متن روشن، حباب ربات سفید/شیری با حاشیهٔ روشن؛ فاصلهٔ خط مناسب RTL و اعداد فارسی.
- کادر پیام: چسبیده به پایین با `safe-area-inset`، دکمهٔ ارسال گرد زرشکی، فوکوس با حلقهٔ ظریف.
- **کنترل صدا بدون جابه‌جایی ✅ (طبق تصمیم شما)**: همان دکمهٔ «خواندن پاسخ» زیر هر پیام و همان کنترل مکث/توقف کنار کادر پیام سرِ جایشان می‌مانند؛ فقط سبک‌شان هماهنگ و ظریف‌تر می‌شود. تمام قابلیت‌های `useAssistantSpeech` حفظ می‌شود.
- چیپ‌های پیشنهاد: هم‌اندازه، تک‌ردیفه با اسکرول افقی در موبایل.
- حالت بارگذاری/خطا: همان سه‌نقطه + یک پیام کوتاه «پاسخ طول کشید — دوباره امتحان کنید» با دکمهٔ «تلاش دوباره».
- معرفی قابلیت‌های فروشگاهی **بدون شلوغ کردن**: ۴ چیپ ثابت و کوچک در حالت خوشامد («جست‌وجوی محصول»، «قیمت و موجودی»، «پیشنهاد طعم»، «طرز تهیه») — نه یک ستون یا کارت بزرگ. (تست smoke هم حداقل ۴ چیپ در خوشامد می‌خواهد.)

### مرحلهٔ ۴ — افزودن به سبد از چت، فقط با تأیید کاربر (قابلیت تازه)
**فایل‌ها:** `src/services/assistant/types.ts` · `engine.ts` · `useAssistantChat.ts` · `AssistantChat.tsx` · `assistant.css` (+ تست جدید در `scripts/smoke.mjs`)
- پاسخ‌هایی که محصول معرفی می‌کنند (`price-stock`، `suggestion`، `catalog`، جست‌وجو) یک **کارت محصول کوچک** می‌گیرند: نام، وزن، قیمت واقعی، موجودی، دکمهٔ گرد «افزودن به سبد».
- با زدن دکمه، **اول یک پیام تأیید** در چت می‌آید: «۱ عدد ژله توت فرنگی ۲۵۰ گرم (۲۰۰٬۰۰۰ تومان) به سبد اضافه شود؟» با دو دکمهٔ «بله، اضافه کن» / «نه».
- فقط بعد از «بله» → `addItemWithToast` از `CartContext` (همان مسیر سبد فعلی، همان `localStorage`، بدون هیچ تغییر دیتابیسی).
- **هیچ Checkout یا پرداخت خودکار اجرا نمی‌شود**؛ بعد از افزودن فقط یک لینک «دیدن سبد خرید» نشان داده می‌شود.
- محصول ناموجود → دکمه غیرفعال + «ناموجود».

---

## ۳) فایل‌هایی که تغییر می‌کنند (جمع‌بندی)

| # | فایل | نوع تغییر |
|---|---|---|
| 1 | `src/admin/pages/AdminDashboardPage.tsx` | بازنویسی چیدمان → ۴ کارت (+ حفظ قرارداد تست) |
| 2 | `src/admin/admin.css` | کلاس‌های تازهٔ کارت/کاشی (افزودنی، بدون حذف کلاس‌های در حال استفاده) |
| 3 | `src/admin/pages/AdminAssistantPage.tsx` | فقط ظاهر آکاردئون‌ها |
| 4 | `src/admin/pages/AdminProductsPage.tsx` | افزودن بخش/تب «موجودی، قیمت، تصویر» (بدون تغییر منطق) |
| 5 | `src/admin/pages/AdminSettingsPage.tsx` | افزودن لینک/بخش «دستور تهیه» و «محتوای سایت» |
| 6 | `src/components/assistant/assistant.css` | پولیش ظاهر + کلاس کارت محصول |
| 7 | `src/components/assistant/AssistantChat.tsx` | جابه‌جایی کنترل صدا، چیپ‌های قابلیت، کارت محصول + تأیید |
| 8 | `src/pages/AssistantPage.tsx` | سربرگ خلوت‌تر |
| 9 | `src/services/assistant/types.ts` | افزودن فیلد اختیاری `products` / `cartIntent` به پاسخ |
| 10 | `src/services/assistant/engine.ts` | چسباندن دادهٔ محصول به پاسخ‌های موجود (متن پاسخ‌ها عوض نمی‌شود) |
| 11 | `src/components/assistant/useAssistantChat.ts` | عبور دادن فیلد تازه + تابع تأیید افزودن به سبد |
| 12 | `scripts/smoke.mjs` | **افزودن** تست برای «افزودن با تأیید» و «عدم Checkout خودکار» (حذف تست موجود: هیچ) |

**دست‌نخورده (قطعی):** `src/pages/*` فروشگاه، `src/components/ProductCard|Header|Footer|Layout`، `src/pages/CartPage.tsx`، `src/pages/CheckoutPage.tsx`، `src/hooks/useCart.ts`، `src/context/CartContext.tsx`، `src/services/supabase*`، `supabase/migrations/*`، `src/App.tsx` (Routing)، `src/services/voiceSettings.ts`، `useAssistantSpeech.ts`، `services/assistant/voice.ts`.

**تغییر دیتابیسی:** **هیچ**. اگر جایی لازم شد (مثلاً ذخیرهٔ تنظیم تازهٔ ربات در جدول)، اول به شما گزارش می‌دهم و منتظر تأیید می‌مانم.

---

## ۴) تست‌هایی که بعد از هر مرحله اجرا می‌شوند
1. `npx tsc --noEmit`
2. `npm run verify:admin` (قرارداد پنل — برچسب‌ها و مسیرهای مستقیم)
3. `npm run build`
4. `npm run smoke` (رندر واقعی همهٔ مسیرها در jsdom + سوئیت چت)
5. `npm run verify:voice` و `npm run test:voice-cloud` / `test:voice-android` (حفظ قابلیت صدا)
6. بررسی چشمی با `npm run dev` روی `/assistant` و `/admin/dashboard` (پیش‌نمایش زنده)

## ۵) ریسک‌ها
- `scripts/smoke.mjs` روی کلاس‌های دقیق چت (`.zhino-assistant-chip`، `.zhino-assistant-ideas-toggle`، `.zhino-assistant-typing`، `.zhino-assistant-signature`، `button[aria-label="ارسال پیام"]`) و «نبود لینک فروشگاه در حالت خوشامد» حساس است → نام کلاس‌ها حفظ می‌شود؛ فقط ظاهر عوض می‌شود.
- `verify-admin.mjs` مسیرهای `?view=prices` / `?view=images` را در داشبورد لازم دارد → به‌شکل لینک ظریف داخل کارت «محصولات» می‌مانند.
- کارت محصول در چت باید در حالت آفلاین هم کار کند (موتور محلی) → داده فقط از `buildAssistantContext()` می‌آید، نه از درخواست تازه.
