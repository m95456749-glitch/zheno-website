# فاز ۹ — صدای فارسی واقعی (بدون نیاز به نصب صدا روی گوشی)

> **وضعیت: آمادهٔ بررسی شما — هیچ Commit، Push، PR یا Merge انجام نشده.**
> همهٔ تغییرات فقط روی فایل‌های کاری برانچ `arena/01a0b174-zheno-website` هستند.

---

## ۱. مشکل و راه‌حل

**مشکل:** Chrome Android روی گوشی شما صدای فارسی ندارد، پس موتور صدای خودِ مرورگر
یا سکوت می‌کرد یا پیام «صدای فارسی را در تنظیمات نصب کنید» نشان می‌داد.

**راه‌حل:** حالا متن پاسخ به یک Edge Function خودمان می‌رود، آنجا با سرویس ابری به
**فایل صوتی MP3 فارسی** تبدیل می‌شود و در صفحه پخش می‌شود. **هیچ صدایی لازم نیست
روی گوشی نصب باشد.**

```
مرورگر ──POST {text}──►  zhino-voice (Edge Function)  ──►  Azure AI Speech
  بدون هیچ کلید            AZURE_SPEECH_KEY اینجا           fa-IR-DilaraNeural
  ◄───── audio/mpeg ─────
```

---

## ۲. بررسی سرویس‌ها (قبل از انتخاب)

| سرویس | صدای فارسی | رایگان | بعد از آن |
| --- | --- | --- | --- |
| **Azure AI Speech** ✅ انتخاب‌شده | `fa-IR-DilaraNeural` (زن) و `fa-IR-FaridNeural` (مرد) — GA و پایدار | **۵۰۰٬۰۰۰ کاراکتر در ماه، بدون انقضا** | ≈ ۱۶ دلار / ۱ میلیون کاراکتر |
| Google Cloud TTS (کلاسیک) | ❌ ندارد — فراخوانی `fa-IR` خطای ۴۰۰ می‌دهد | ۴ میلیون (زبان‌های دیگر) | ۴ دلار / میلیون |
| Google Gemini TTS | ✅ ولی در وضعیت Preview | سهمیهٔ کم | ۱۰ دلار / میلیون توکن صوتی |
| OpenAI TTS | ⚠️ فارسی با لهجهٔ انگلیسی | فقط ۵ دلار اعتبار اولیه | ۱۵ دلار / میلیون |

**چرا Azure:** تنها سرویسی که صدای بومی فارسی با کیفیت GA دارد، سهمیهٔ رایگانش
منقضی نمی‌شود، و مستقیماً MP3 برمی‌گرداند.

**تخمین مصرف شما:** هر پاسخ دستیار حدود ۲۰۰ کاراکتر است → سهمیهٔ رایگان تقریباً
**۲٬۵۰۰ پاسخ در ماه**. ضمناً «پخش مجدد» همان پاسخ از حافظه می‌آید و هیچ کاراکتر
تازه‌ای مصرف نمی‌کند.

---

## ۳. کاری که باید شما انجام دهید (۳ دقیقه)

بدون این مرحله سایت **دقیقاً مثل الان** کار می‌کند (هیچ چیزی خراب نمی‌شود)؛ فقط
صدای فارسی ابری فعال نمی‌شود.

1. در [portal.azure.com](https://portal.azure.com) یک منبع **Speech service** بسازید
   و لایهٔ **Free (F0)** را انتخاب کنید.
2. از صفحهٔ **Keys and Endpoint** مقدار `KEY 1` و `Location` را بردارید.
3. در Supabase → Edge Functions → Secrets:

```bash
supabase secrets set AZURE_SPEECH_KEY="<کلید>" AZURE_SPEECH_REGION="westeurope"
supabase functions deploy zhino-voice
```

> **کلید فقط در Supabase Secrets می‌ماند.** در `.env`، در کد، در بستهٔ نهایی و در
> GitHub هیچ‌جا نوشته نشده — این موضوع با تست خودکار قفل شده است (بخش ۵).

صدای پیش‌فرض **دلارا (زن)** است، همان‌طور که انتخاب کردید. برای تغییر به صدای مرد
بدون هیچ تغییری در کد:

```bash
supabase secrets set AZURE_SPEECH_VOICE="fa-IR-FaridNeural"
```

---

## ۴. چه چیزی تغییر کرد و چه چیزی دست نخورد

### فایل‌های جدید
| فایل | نقش |
| --- | --- |
| `supabase/functions/zhino-voice/index.ts` | Edge Function: متن → MP3 فارسی. کلید فقط اینجا. |
| `supabase/functions/zhino-voice/README.md` | راهنمای نصب، هزینه، محدودیت‌ها، نکات امنیتی |
| `src/services/assistant/voice.ts` | لایهٔ اتصال مرورگر (بدون هیچ کلیدی) |
| `src/components/assistant/useAssistantCloudVoice.ts` | پخش فایل صوتی + مکث/ادامه + کَش حافظه |
| `src/components/assistant/useAssistantVoice.ts` | انتخاب بین صدای ابری و صدای گوشی |
| `src/components/assistant/speechText.ts` | تکه‌کردن متن روی مرز جمله (مشترک بین هر دو مسیر) |
| `scripts/tts-cloud-sim.mjs` | تست خودکار مسیر ابری (۷ سناریو) |

### فایل‌های تغییریافته
| فایل | تغییر |
| --- | --- |
| `AssistantChat.tsx` | فقط اتصال به هوک تازه + حالت «در حال آماده‌سازی صدا…» |
| `assistant.css` | ۱۱ خط برای همان حالت آماده‌سازی |
| `useAssistantSpeech.ts` | فقط جابه‌جایی: تابع تکه‌کنندهٔ متن به `speechText.ts` منتقل شد (رفتار **دقیقاً** یکسان، با تست تفاضلی تأیید شد) |
| `README.md` · `ASSISTANT_AI.md` · `SECURITY-AUDIT.md` | به‌روزرسانی مستندات برای مسیر ابری |
| `.env.example` | توضیح `VITE_VOICE_API_URL` (اختیاری) |
| `scripts/verify-supabase.mjs` | ۱۸ بررسی امنیتی تازه |
| `scripts/smoke.mjs` | ۳۲ بررسی تازه برای Edge Function صدا |
| `.github/workflows/ci.yml` | اجرای دو تست صدا در CI |
| `package.json` | اسکریپت `test:voice-cloud` |

### دست‌نخورده (تأیید شده با `git status`)
✅ دیتابیس و Migrations · ✅ سیاست‌های RLS · ✅ پنل مدیریت · ✅ سبد خرید و پرداخت
· ✅ منطق و لحن پاسخ ربات · ✅ ظاهر سایت · ✅ `supabaseClient.ts`

**دکمه‌های پخش، توقف و پخش مجدد همگی حفظ شدند** و حالا روی خودِ فایل صوتی کار
می‌کنند (مکث واقعی، نه شبیه‌سازی).

---

## ۵. تست‌ها — چه چیزی واقعاً تست شد

همهٔ تست‌ها **آفلاین** اجرا می‌شوند: هیچ تماسی با Azure گرفته نشد و هیچ هزینه‌ای
ایجاد نشد.

| مجموعه | نتیجه |
| --- | --- |
| `npm run typecheck` | ✅ بدون خطا |
| `npm run verify:data` | ✅ همه پاس |
| `npm run verify:supabase` | ✅ همه پاس (۵۰ بررسی؛ **۱۸ بررسی تازهٔ فاز ۹**) |
| `npm run build` | ✅ ساخته شد |
| `npm run smoke` | ✅ همه پاس (۴۴۱ بررسی؛ **۳۲ بررسی تازهٔ فاز ۹**) |
| `npm run test:voice-android` | ✅ هر ۵ سناریوی صدای گوشی — ۳۵ بررسی، بدون تغییر رفتار |
| `npm run test:voice-cloud` | ✅ هر ۷ سناریوی صدای ابری — ۴۲ بررسی (۵ بار پشت‌سرهم، پایدار) |

### ✅ تست‌شدهٔ خودکار — رفتار محصول
- **گوشی بدون صدای فارسی، پاسخ فارسی را می‌شنود** (سناریوی C1 — دقیقاً مشکل شما)
- پیام «صدای فارسی را در تنظیمات نصب کنید» دیگر ظاهر نمی‌شود
- پخش / مکث / ادامه / توقف روی فایل صوتی درست کار می‌کنند
- «پخش مجدد» از حافظه می‌آید → **درخواست دوم و هزینهٔ اضافه ندارد**
- اگر سرویس ابری خطا داد → بی‌صدا سراغ صدای گوشی می‌رود؛ اگر آن هم نبود، همان
  پیام کوتاه قبلی. **چت هرگز نمی‌شکند.**
- اگر Secret تنظیم نشده باشد → رفتار **دقیقاً** مثل قبل از فاز ۹ و حتی یک درخواست
  شبکه هم زده نمی‌شود
- **پاسخ بلند تا آخر خوانده می‌شود** (سناریوی C7) — هیچ جمله‌ای نصفه رها نمی‌شود

### ✅ تست‌شدهٔ خودکار — امنیت
- کلید Azure در بستهٔ نهایی (`dist/index.html`) **صفر بار** پیدا شد
- مرورگر هرگز مستقیم با Azure حرف نمی‌زند — فقط با Edge Function خودمان
- فقط **متن پاسخ ربات** فرستاده می‌شود؛ **هرگز سؤال کاربر، نام، نشانی یا سبد خرید**
- متن پیش از SSML کاملاً escape می‌شود → یک پاسخ نمی‌تواند صدا را عوض کند یا تگ
  تزریق کند (این را با یک حملهٔ واقعی تست کردیم)
- خطای خام سرویس‌دهنده و کلید هرگز به مرورگر برنمی‌گردد
- تابع صدا **به هیچ جدولی دسترسی ندارد** — حتی یک `SELECT` هم نمی‌زند
- محدودیت‌ها فعال: ۱۲۰۰ کاراکتر متن، ۸KB بدنه، ۴۰ درخواست در ۵ دقیقه، CORS

---

## ۵٫۱ یک ایراد که در بازبینی پیدا و رفع شد

بعد از آمادهٔ‌شدن کار، خودم کد را دوباره بازبینی کردم و یک ایراد واقعی پیدا شد که
هیچ‌کدام از تست‌های آن لحظه آن را نمی‌گرفت:

> **پاسخ‌های بلند بی‌صدا نصفه می‌ماندند.** ربات تا ۴۰۰۰ نویسه پاسخ می‌دهد، ولی هر
> درخواست به سرویس صدا سقف ۱۲۰۰ نویسه دارد. مسیر ابری کل متن را در یک درخواست
> می‌فرستاد، پس یک پاسخ ۲۵۰۰ نویسه‌ای **وسط جمله قطع می‌شد** و بقیه‌اش هرگز
> خوانده نمی‌شد — بدون هیچ پیام خطایی. (مسیر صدای گوشی این مشکل را نداشت و کل
> پاسخ را می‌خواند.)

اندازه‌گیری واقعی روی یک پاسخ ۲۵۲۰ نویسه‌ای پیش از رفع: **۱۳۲۰ نویسه خوانده
نمی‌شد.**

**رفع:** متن حالا روی **مرز جمله** تکه می‌شود و تکه‌ها پشت‌سرهم پخش می‌شوند؛ تکهٔ
بعدی همزمان با پخش تکهٔ فعلی ساخته می‌شود تا بین جمله‌ها سکوت نیفتد. همان تابعی
که مسیر صدای گوشی از قبل استفاده می‌کرد، حالا مشترک است (`speechText.ts`) — پس
هر دو مسیر دقیقاً یک‌جور متن را می‌شکنند.

**قفل‌شدن با تست:** سناریوی **C7** یک پاسخ بلند واقعی می‌فرستد و بررسی می‌کند که
تک‌تک نویسه‌های پاسخ به سرویس صدا رسیده باشند، هیچ تکه‌ای از سقف سرویس رد نشود، و
تکه‌ها واقعاً پشت‌سرهم پخش شوند. این تست را روی کد قدیمی هم اجرا کردم تا مطمئن
شوم واقعاً ایراد را می‌گیرد: **شکست خورد** (همان‌طور که باید). دو بررسی ساختاری
هم به `verify:supabase` اضافه شد تا این ایراد دوباره برنگردد.

---

## ۶. ⚠️ چیزهایی که حتماً باید روی گوشی خودتان تست شوند

این‌ها را هیچ تست خودکاری نمی‌تواند ثابت کند، چون به سخت‌افزار و سرویس واقعی نیاز دارند:

| # | مورد | چطور تست کنید |
| --- | --- | --- |
| ۱ | **کیفیت و لهجهٔ واقعی صدای دلارا** | یک سؤال بپرسید و گوش کنید. اگر لحن را نپسندیدید، با یک Secret به `fa-IR-FaridNeural` تغییر دهید. |
| ۲ | **اتصال واقعی به Azure** | همهٔ تست‌ها با سرویس شبیه‌سازی‌شده بودند. کلید و ناحیهٔ واقعی باید یک‌بار امتحان شوند. |
| ۳ | **تأخیر شروع صدا روی شبکهٔ موبایل** | حدود ۱ تا ۳ ثانیه انتظار می‌رود. حین انتظار، «در حال آماده‌سازی صدا…» نشان داده می‌شود. |
| ۴ | **سیاست Autoplay روی Chrome Android واقعی** | کد اجازهٔ پخش را در همان لمس کاربر می‌گیرد، ولی رفتار واقعی مرورگر فقط روی دستگاه قطعی می‌شود. |
| ۵ | **پخش هنگام قفل‌شدن صفحه / تماس ورودی** | رفتار سیستم‌عامل است؛ کد با ترک صفحه صدا را قطع می‌کند. |
| ۶ | **موجودبودن صدای فارسی در ناحیهٔ انتخابی** | اگر ناحیه‌تان `fa-IR` نداشت، پاسخ `GET` تابع `ready:false` می‌دهد و سایت بی‌صدا به حالت قبل برمی‌گردد. |

**ترتیب پیشنهادی تست روی گوشی:** Secret را بگذارید → تابع را Deploy کنید → صفحهٔ
دستیار را باز کنید → دکمهٔ صدا را روشن کنید → یک سؤال بپرسید → مکث/ادامه/توقف را
بزنید → «خواندن پاسخ» را دوباره بزنید (باید فوری و بدون تأخیر پخش شود، چون از
حافظه می‌آید).

---

## ۷. اگر نتیجه را پسندیدید

```bash
git add -A
git commit -m "phase 9: real Persian cloud voice via Supabase Edge Function"
git push origin arena/01a0b174-zheno-website
```

اگر نپسندیدید، چون هیچ Commit‌ای نشده، با یک `git checkout .` همه‌چیز به حالت قبل
برمی‌گردد.
