// ============================================================
// ZHINO — «دستیار ژینو» (فاز ۵) — هویت بصری دستیار
//
// از فاز ۴ فقط همین هویت مانده است: نام، شعار و تصویر اختصاصی ربات
// (فایل واقعی: public/images/assistant/بارگیری.jpeg — ۱۰۲۴×۱۵۳۶).
// منطق پاسخ‌ها به src/services/assistant/ منتقل شده تا موتور محلی و
// اتصال به مدل هوش مصنوعی یک قرارداد مشترک داشته باشند.
//
// آدرس تصویر نسبت به محل نصب سایت ساخته می‌شود، پس روی دامنهٔ اصلی
// و روی مسیر مخزن هر دو درست باز می‌شود.
// ============================================================

import { withSiteBase } from '../../utils/siteBase';

/** نام نمایشی دستیار */
export const ASSISTANT_NAME = 'دستیار ژینو';

/** شعار کوتاه — سربرگ صفحه و راهنمای دکمهٔ شناور */
export const ASSISTANT_TAGLINE = 'راهنمای انتخاب، تهیه و خرید دسرهای ژینو';

/**
 * تصویر واقعی ربات دستیار — یک هویت بصری واحد برای دکمهٔ شناور،
 * سربرگ صفحه، آواتار پیام‌ها، خوش‌آمدگویی و صحنهٔ استودیو.
 * همین فایل، بدون هیچ بازطراحی یا فیلتری، با نسبت اصلی نمایش داده می‌شود.
 */
export const ASSISTANT_BOT_IMAGE = withSiteBase(encodeURI('images/assistant/بارگیری.jpeg'));
