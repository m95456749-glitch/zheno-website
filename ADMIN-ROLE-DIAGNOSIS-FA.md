# گزارش تشخیص و رفع خطای «دسترسی مدیر تأیید نشد» در آپلود تصویر

تاریخ: ۲۶ سپتامبر ۲۰۲۶ · شاخه: `arena/01a0ddc9-zheno-website`
محدوده: فقط مشکل احراز هویت مدیر در `/admin/product-images` — بدون Migration جدید روی Production، بدون هیچ تغییر در RLS یا RPCهای دیتابیس.

## ۱. خطا دقیقاً از کجا می‌آمد؟

پیام «دسترسی مدیر تأیید نشد؛ فقط حساب دارای نقش admin می‌تواند تصویرها را تغییر دهد.» در
`describeImageError` (فایل `src/services/supabaseProductImages.ts`) برای خطاهای از جنس
`row-level security / permission denied / 42501` ساخته می‌شود؛ یعنی آپلود در لایهٔ
Storage یا دیتابیس رد شده است.

## ۲. نقش admin دقیقاً از کجا خوانده می‌شود؟

| لایه | منبع خواندن نقش |
|---|---|
| دیتابیس (یگانه مرجع اعمال): `manage_product_image`، پالیسی‌های RLS جدول `product_images` و Storage (تابع `product_image_upload_allowed`) | `public.is_admin()` = `coalesce((auth.jwt() -> 'app_metadata' ->> 'role') = 'admin', false)` — یعنی **کلیِم `app_metadata.role` در JWT درخواست**؛ این کلیِم عکسِ لحظهٔ صدور/تازه‌سازی توکن از `auth.users.raw_app_meta_data` است. تعریف این تابع با fingerprint قرارداد نسخهٔ ۳ قفل شده و تغییرپذیر نیست (و نیازی هم به تغییر نداشت). |
| فرانت‌اند (قدیم): ورود و بازیابی نشست پنل | فقط RPC `is_admin()` — همان عکسِ داخل JWT، بدون هیچ مراجعه‌ای به رکورد زندهٔ کاربر |

## ۳. چرا کاربر فعلی «مدیر» تشخیص داده نمی‌شد؟

چون درِ پنل فقط به **عکس داخل توکن** اعتماد می‌کرد و هرگز رکورد زندهٔ کاربر را نمی‌پرسید،
سه سناریوی واقعی — با پیام خطای یکسان و گمراه‌کنندهٔ بالا — ممکن بود:

1. **نقش در جای اشتباه ثبت شده بود.** اگر `{"role":"admin"}` در `user_metadata`
   (ویرایشگر «User metadata» داشبورد) ثبت شده باشد، `is_admin()` آن را نمی‌بیند؛
   چون فقط `app_metadata` را می‌خواند. کد قدیم هیچ راهی برای تشخیص این جابه‌جایی نداشت.
2. **عکسِ قدیمی در توکن.** اگر توکن نشست **قبل از اعطای نقش** صادر شده باشد،
   `app_metadata.role` در آن خالی است؛ در حالی که رکورد زندهٔ کاربر مدیر است.
3. **افتادن بی‌صدای نشست.** بررسی کد `supabase-js@2.116` (توابع `fetchWithAuth` و
   `_getSessionToken`) نشان می‌دهد اگر در لحظهٔ درخواست نشست معتبری وجود نداشته باشد،
   کتابخانه به‌جای خطا، **کلید publishable را به‌عنوان Bearer می‌فرستد — یعنی درخواست
   ناشناس (anon) می‌رود**. آپلود ناشناس به سطل `product-images` درست با همین پیام
   «نقض RLS» رد می‌شود؛ پس پیام لزوماً به معنای «حساب شما مدیر نیست» نبود.

نتیجه: کاربر وارد پنل می‌شد، اما در لحظهٔ آپلود، درخواستش یا با توکن بدون کلیِم نقش
یا به‌صورت ناشناس می‌رفت و خطای مبهم «دسترسی مدیر تأیید نشد» می‌دید — بدون هیچ
تشخیص، ترمیم یا راهنمایی.

## ۴. راه‌حل پیاده‌شده (فقط فرانت‌اند؛ بدون Migration)

فایل جدید `src/services/supabaseAdminRole.ts` + تغییر دو فایل موجود:

1. **اعتبارسنجی نقش از خود Supabase، به‌صورت زنده و چندلایه** (`checkAdminAccess`):
   - نشست فعلی → قضاوت دیتابیس با همان `is_admin()` (همان چیزی که هر نوشت با آن روبه‌روست)
     → سپس **رکورد زندهٔ کاربر با `auth.getUser()`** (ردیف واقعی `auth.users`، نه عکس توکن).
   - اگر رکورد زنده مدیر نباشد → ورود رد می‌شود؛ حتی اگر توکن قدیمی هنوز کلیِم نقش داشته
     باشد (پوشش «نقش پس گرفته‌شده»).
   - اگر رکورد زنده مدیر باشد اما توکن کلیِم نداشته باشد → `refreshSession()` توکن را از
     سرور دوباره صادر می‌کند (سرور کلیِم‌های روز را در توکن جدید می‌گذارد) و دوباره از
     دیتابیس پرسیده می‌شود. در همهٔ حالت‌های نامشخص، **شکست بسته (fail-closed)**.
2. **تشخیص دقیق به‌جای خطای مبهم**: اگر نقش در `user_metadata` باشد، پیام ورود دقیقاً
   می‌گوید «نقش در `user_metadata` ثبت شده؛ باید در `app_metadata` همین کاربر ثبت شود»
   همراه با مسیر داشبورد/Management API.
3. **ترمیم یک‌بارهٔ نوشت‌های ردشده** (`supabaseProductImages.ts`): وقتی آپلود یا
   `manage_product_image` با خطای «دسترسی/RLS» رد می‌شود، نشست یک‌بار با همان روش بالا
   راستی‌آزمایی/ترمیم می‌شود؛ اگر درست شد **دقیقاً یک بار** با همان بایت/مسیر/شناسهٔ
   عملیات تکرار می‌شود (آپلود `upsert: false` و RPC با رسیدِ عملیات، ذاتاً ایمن در برابر
   تکرارند). اگر نشست وجود نداشته باشد یا حساب واقعاً مدیر نباشد، به‌جای پیام مبهم،
   علت واقعی گفته می‌شود («نشست پایان یافته؛ دوباره وارد شوید» یا همان تشخیص نقش).

**امنیت:** هیچ سیاست RLS، تابع، گرنت یا حصار امنیتی تغییر نکرد. این تغییر هیچ دسترسی‌ای
«نمی‌دهد» — فقط ورودیِ درست را به همان داورِ همیشگی (دیتابیس) می‌رساند و علت رد را دقیق
گزارش می‌کند. هیچ bypass، hardcoded admin یا حذف بررسی نقش وجود ندارد (سنجهٔ ساختاری
جدید در `verify:admin` این را پایدار می‌کند). هیچ کلید سرویسی به فرانت‌اند نیامده است.

## ۵. اقدام لازم سمت حساب کاربری (بدون Migration)

اگر خطا همچنان ادامه داشت، تنها اقدام لازم روی همان حساب در داشبورد یا Management API:

- `auth.users.raw_app_meta_data` کاربر باید دقیقاً حاوی `{"role":"admin"}` باشد
  (نه `raw_user_meta_data`). پس از تغییر، یک بار خروج/ورود کافی است؛ پنل تازه توکن
  را خودش تازه‌سازی می‌کند.

## ۶. نتیجهٔ تست‌ها (خروجی واقعی در همین شاخه)

| فرمان | نتیجه |
|---|---|
| `npm run typecheck` | ✅ بدون خطا |
| `npm run build` | ✅ ساخت موفق؛ `dist/index.html = 1,039.88 kB` (gzip: 273.46 kB) |
| `npm run smoke` | ✅ همهٔ بررسی‌ها پاس شد — شامل ۳ سناریوی جدید: تشخیص نقش در `user_metadata` و رد ورود با نام‌بردن از آن؛ ترمیم نشستِ قبل‌از‌اعطای‌نقش و باز شدن پنل؛ آپلودِ ترمیم‌شده فقط با JWT تازهٔ مدیر (هرگز ناشناس/قدیمی نیست) |
| `npm run test:images` | ✅ ۲۲ بررسی سرویس (شامل ۲ مورد جدید: ردِ بدون نشست → پیام «پایان نشست» بدون تکرار کور؛ و `admin_required` بدون نشست) + ۳۳ بررسی PostgreSQL محلی |
| `npm run test:images:migration` | ✅ ۲۳ بررسی امنیت/حفظ دادهٔ نسخهٔ ۳ (بدون تغییر، تأیید عدم نیاز به migration جدید) |
| `npm run verify:admin` | ✅ همهٔ قراردادهای مدیریت، شامل سنجه‌های جدید «بدون bypass / بدون admin سخت‌کدشده» |
| `npm run verify:supabase` / `verify:data` / `verify:images:bundle` | ✅ پاس |

**محدودیت صادقانه:** این تست‌ها شبکهٔ واقعی/سرویس Storage ابری را اجرا نمی‌کنند
(مانند قبل، شبکه شبیه‌سازی‌شده و PostgreSQL محلی است). سناریوی «ترمیم توکن کهنه» در
شبیه‌ساز کامل پوشش داده شده؛ تأیید نهایی روی پروژهٔ واقعی با یک آپلود ساده پس از
اعمال این نسخه انجام می‌شود.

## ۷. فایل‌های تغییرکرده

- **جدید:** `src/services/supabaseAdminRole.ts` — اعتبارسنجی زندهٔ نقش + تشخیص‌ها
- `src/admin/auth/supabaseAuth.ts` — ورود/بازیابی/رویدادها از مسیر `checkAdminAccess`
- `src/services/supabaseProductImages.ts` — طبقه‌بندی ردِ دسترسی، ترمیم یک‌باره، پیام صادق
- `scripts/smoke.mjs` — پشتیبانی `/auth/v1/user` در شبیه‌ساز + ۲ سناریوی جدید
- `scripts/test-product-images.mjs` — ۲ تست جدید برای حالت بدون نشست
- `scripts/verify-admin.mjs` — سنجه‌های قراردادی جدید (بدون ضعف‌بخشی)
- `README.md` — راهنمای دقیق `app_metadata` در برابر `user_metadata`
- **جدید:** `ADMIN-ROLE-DIAGNOSIS-FA.md` — همین گزارش

هیچ فایل `supabase/migrations/*` تغییر نکرد و هیچ migration جدیدی آماده/اجرا نشده است.
